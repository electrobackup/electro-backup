c
с
Order.new
c
product_stock.amount
product_stock = Stock.find_by(product_id: item["id"])
item
product_stock = Stock.find_by(product_id: item["id"])
Stock.find(item["id"])
product = Product.find(item["id"])
c
product.stocks.find{ |ps| ps.size == item["size"] }
product
product = Product.find(item["id"])
product.
product.stocks.find{ |ps| ps.size == item["size"] }
Product.find(item["id"])
